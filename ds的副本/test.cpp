#include <iostream>
#include <string>
using namespace std;


  int main() {

    string a;
    cin>>a;
    cout<<a<<endl;

    cout<<a.size()<<endl;
      return 0;
  }
  //
  // Noder head = new Node;
  // Noder ptr = head;
  // char l;
  // cin>>l;
  // ptr->letter = l;
  //
  // while(l = getch() != EOF) {
  //
  //   ptr->next = new Node;
  //   ptr = ptr->next;
  //   ptr->letter = l;
  //
  // }
  //
  // ptr = head;
  //
  // // cin>>n;
  //
  // while(ptr) {
  //   cout<<ptr->letter;
  //   ptr = ptr->next;
  // }
